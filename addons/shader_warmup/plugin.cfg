[plugin]

name="Shader Warmup"
description="Scans the project for materials and shaders and regenerates res://generated/shader_warmup_manifest.tres, so the startup warmup always knows about everything that needs compiling."
author="FeverDreamDev"
version="1.0"
script="plugin.gd"
